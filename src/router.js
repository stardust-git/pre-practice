import Vue from 'vue';
import VueRouter from 'vue-router';
Vue.use(VueRouter);

// console.log("router is okk")
const routes = [

]
 const router = new VueRouter({
    // mode: 'history',
  routes 
})

export default router;